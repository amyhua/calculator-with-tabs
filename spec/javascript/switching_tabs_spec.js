describe('Switching Tabs: ', function(){

  var calc1 = { currentDisplay: '123', lastOperator: 'plus' };

  it('clicking on an active tab should keep that tab active and not change the calculator', function(){
  });

  describe('clicking on a non-active tab', function(){

    it('should make that tab active and switch out the calculator data accordingly', function(){
      // display
      // last operator

    });
  });
});